#workout-week
  - @period.each do |day|
    .day{ :class => "flexbox", :id => day[:node] }
      %section.date
        .day-of-week
          = day[:week_day]
        .full-date
          = day[:formatted]

